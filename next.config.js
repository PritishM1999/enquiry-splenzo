/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['demosplen.urips.co.in'],
        unoptimized: true,
    },
    output: 'export',

}

module.exports = nextConfig

